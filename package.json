{
  "name": "vac-dsl",
  "version": "0.1.0",
  "scripts": {
    "fmt": "prettier --write --list-different --single-quote --no-semi \"**/*.?(m)js\" \"!**/.esm-cache/**\" || true"
  },
  "devDependencies": {
    "lerna": "^2.5.1",
    "prettier": "^1.8.1"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ]
}
