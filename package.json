{
  "name": "vac-dsl",
  "version": "0.1.0",
  "scripts": {
    "fmt": "prettier --write --list-different --single-quote --no-semi \"**/*.?(m)js\" \"!**/.esm-cache/**\" || true"
  },
  "devDependencies": {
    "lerna": "^2.5.1",
    "plop": "^1.9.0"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ]
}
