import { AST, Command } from '@vac-dsl/core'
import {{camelCase (unscope name)}} from './index'

// TODO: write tests for {{camelCase (unscope name)}}
test('dummy test', () => {
  expect(true).toBe(true)
})
